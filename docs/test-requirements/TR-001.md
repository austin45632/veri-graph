# TR-001 驗證需求：REQ-001 的追溯完整性

## Source Requirement

REQ-001

## Verification Rule

檢查每個 normative `REQ-*` 是否至少關聯一個 `TR-*`，且每個 `TR-*` 是否至少關聯一個 `TC-*`。

## Role In V1

`TR` 在 v1 作為 Codebeamer 中的獨立 tracker，負責承接驗證需求、覆蓋狀態與 `REQ -> TR -> TC` 追溯。
