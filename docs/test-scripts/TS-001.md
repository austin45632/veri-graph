# TS-001 vTESTstudio Test Asset Index Example

## Purpose

說明 `TS` 在 v1 應對應到 vTESTstudio 中的可執行測試資產，而不是 repo 內的正式腳本文本。

## Asset Mapping

- Linked testcase: `TC-001`
- Linked test requirement: `TR-001`
- Execution system: `vTESTstudio`
- Asset identifier: `VT-PLACEHOLDER-001`

## Integration Notes

- `TS` 的正式主體在 vTESTstudio。
- repo 中的 `TS-*` 文件只保留索引、整合說明或外部 ID。
- 正式追溯應維持 `TR -> TC -> TS` 的可查詢對應關係。
