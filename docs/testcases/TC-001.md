# TC-001 檢查需求追溯關聯

## Objective

驗證 `REQ-001` 在追溯矩陣中有對應 `TR-001`，且 `TR-001` 能對應 `TC-001`，並由 `TC-001` 連到 `TS-001`。

## Preconditions

- trace-matrix.md 已更新

## Steps

1. 讀取 trace matrix
2. 查找 source_id=`REQ-001` 的記錄
3. 確認 `REQ-001` 以 relation=`validated_by` 連到 `TR-001`
4. 確認 `TR-001` 以 relation=`verified_by` 連到 `TC-001`
5. 確認 `TC-001` 以 relation=`implemented_by` 連到 `TS-001`

## Expected Result

REQ-001 不為 non-compliant。
